box:
  id: golang

build:
  steps:
    # Gets the dependencies
    - script:
        name: get hugo
        code: |
          go get github.com/gohugoio/hugo

    # Build the project
    - script:
        name: build site
        code: |
          cd exampleSite && hugo